
#define DEBUG
#include "pathfinder.h"
#include "parser.h"
#include "mover.h"


task main() {
     parse();
     find_path(1, 6, table_1);
     StartTask(checkRpm);
     StartTask(calculatePosition);
     movePath(true);
}
